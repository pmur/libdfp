name: libdfp CI on x86-64

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:

    runs-on: [ubuntu-latest, ubuntu-24.04-ppc64le-p10, ubuntu-24.04-s390x, ubuntu-24.04-ppc64le]

    steps:
    - uses: actions/checkout@v4

    - name: Configure libdfp
      run: |
        mkdir build
        cd build
        ../configure

    - name: Build libdfp
      working-directory: build
      run: make -j4

    - name: Run libdfp test suite
      working-directory: build
      run: make check -j4
